<UserControl x:Class="MEC.Views.DispatchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MEC"
             xmlns:views="clr-namespace:MEC.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Unloaded="UserControl_Unloaded">
    <UserControl.DataContext>
        <views:DispatchViewModel />
    </UserControl.DataContext>
    <Border>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="{Binding DispatchMessage}" FontSize="24" Margin="15"/>
            <Label Content="{Binding Pseudo}" FontSize="48" HorizontalAlignment="Center"/>
            <Button Command="{Binding DispatchPressedCommand}" IsEnabled="{Binding DispatchEnabled}" Content="DISPATCH" MinWidth="100" MinHeight="75" FontSize="24" Margin="15" HorizontalAlignment="Center" BorderThickness="2" />
        </StackPanel>
    </Border>
</UserControl>