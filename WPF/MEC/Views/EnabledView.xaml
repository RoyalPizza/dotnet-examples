<UserControl x:Class="MEC.Views.EnabledView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MEC"
             xmlns:views="clr-namespace:MEC.Views"
             mc:Ignorable="d"
             d:DesignHeight="768" d:DesignWidth="1024">
    <UserControl.DataContext>
        <views:EnabledViewModel />
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <views:DispatchView Grid.Row="0" Margin="25" />

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="25,5,25,5">
                <Button Content="Logs" FontSize="24" MinWidth="100" MinHeight="75" Padding="15" Margin="5,5,15,5" Command="{Binding LogsPressedCommand}" />
                <Button Content="Trouble" FontSize="24" MinWidth="100" MinHeight="75" Padding="15" Margin="5,5,15,5" Command="{Binding TroublePressedCommand}"/>
            </StackPanel>

            <views:HistoryView Grid.Row="2" Margin="25" />

        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="2*" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Content="Disable" Command="{Binding DisablePressedCommand}" VerticalAlignment="Center" HorizontalAlignment="Right"  MinWidth="100" MinHeight="75" Margin="10" FontSize="24" />
                <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="25">
                    <Button Content="Flight" FontSize="24" MinWidth="100" MinHeight="75" Padding="15" Margin="5,5,15,5" Command="{Binding FlightPressedCommand}" />
                    <Button Content="Carrier" FontSize="24" MinWidth="100" MinHeight="75" Padding="15" Margin="5,5,15,5" Command="{Binding CarrierPressedCommand}" />
                    <Button Content="Pier" FontSize="24" MinWidth="100" MinHeight="75" Padding="15" Margin="5,5,15,5" Command="{Binding PierPressedCommand}" />
                </StackPanel>
            </Grid>

            <views:NumberpadView Grid.Row="1" Margin="25,0,25,25" />
        </Grid>

    </Grid>
</UserControl>