<Window x:Class="MEC.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MEC"
        mc:Ignorable="d"
        Title="MEC" Height="768" Width="1024"
        Loaded="Window_Loaded" Unloaded="Window_Unloaded"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ContentControl Name="MainContentControl" Content="{Binding CurrentView}" Grid.Row="0" />
        <StatusBar Grid.Row="1">
            <StatusBarItem HorizontalAlignment="Left">
                <Label Content="{Binding StationName}" />
            </StatusBarItem>

            <StatusBarItem HorizontalAlignment="Right">
                <Label Content="{Binding StatusBarClock}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>