<Window x:Class="MEC.Views.CarrierView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MEC.Views"
        xmlns:views="clr-namespace:MEC.Views"
        mc:Ignorable="d"
        Title="Carriers" Height="450" Width="400"
        Loaded="Window_Loaded"
        WindowStartupLocation="CenterScreen" WindowStyle="SingleBorderWindow" ResizeMode="NoResize">
    <Window.DataContext>
        <views:CarrierViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <ListBox Grid.Row="0" ItemsSource="{Binding Carriers}" SelectedItem="{Binding SelectedCarrier, Mode=TwoWay}" FontSize="24">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" Margin="5" />
                        <TextBlock Text="{Binding Code}" Margin="5" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Cancel" MinHeight="50" MinWidth="100" Command="{Binding CancelPressedCommand}" Margin="5"/>
            <Button Content="Confirm" MinHeight="50" MinWidth="100" Command="{Binding ConfirmPressedCommand}" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
